/* いいね数(likes_count)の多い順にテーブルで表示する */
%table.table
  %tr
    %th user_name
    %th title
    %th content
    %th likes_count
  - @ranks.each do |rank|
    %tr
      %td= User.find(rank.user_id).name
      %td= link_to rank.title, article_path(rank.id)
      %td!= rank.content
      %td= rank.likes_count